Options -Indexes
RewriteEngine On

# uploads/ と themes/ 以下の静的ファイルはそのまま配信する
RewriteRule ^(uploads|themes)/ - [L]

# 存在するファイル・ディレクトリはそのまま配信する
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# それ以外は index.php に転送する
RewriteRule ^ index.php [L]
